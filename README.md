# gifConverter

### pre-requisites

- if you have a mac you need to install `ffmpeg`
  ```sh
    brew install ffmpeg
  ```

# How to run

- pull down the repo
- install all packages
  ```sh
  npm i
  ```
